import { PatientCardClickableDirective } from './patient-card-clickable.directive';

describe('PatientCardClickableDirective', () => {
  it('should create an instance', () => {
    const directive = new PatientCardClickableDirective();
    expect(directive).toBeTruthy();
  });
});
